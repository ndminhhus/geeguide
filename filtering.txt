# Objective: Filtering image collection base on:
- Time of interest
- Area of interest
- Metadata (cloud cover, path, row, etc.)

# Core script
var L8_col = l8toa.filterBounds(bekaa)
                .filterDate(start,end)
                .filter(ee.Filter.eq('WRS_PATH',174)).filter(ee.Filter.eq('WRS_ROW',37))
                .filter(ee.Filter.lt('CLOUD_COVER',95))
